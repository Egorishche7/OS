#pragma once

#include <iostream>

using namespace std;

class Database
{
public:
    // Returns the user type based on the username
    // Returning constants here is just an example
    string getUserType(const string&);
};